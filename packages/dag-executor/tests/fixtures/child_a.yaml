name: Child A Workflow
config:
  checkpoint_prefix: child-a

inputs:
  arg0:
    type: string
    required: true

nodes:
  - id: process
    name: Process Data
    type: bash
    script: echo "Processing A"

outputs:
  result:
    node: process
