name: test_with_notifications
config:
  checkpoint_prefix: test-notif
  notifications:
    slack:
      events:
        - gate_pending
        - workflow_failed
        - workflow_completed
      webhook_url_env: SLACK_WEBHOOK_URL
nodes:
  - id: step1
    name: Test Step
    type: bash
    script: echo "test"
