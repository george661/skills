name: Prompt Writes Text Test
config:
  checkpoint_prefix: test
  worktree: false

state:
  result:
    type: channel
    default: null

nodes:
  - id: prompt1
    name: Text Prompt
    type: prompt
    prompt: "What is 2+2?"
    model: sonnet
    output_format: text
    writes:
      - result
