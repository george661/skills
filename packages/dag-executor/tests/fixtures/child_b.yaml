name: Child B Workflow
config:
  checkpoint_prefix: child-b

inputs:
  arg0:
    type: string
    required: true

nodes:
  - id: process
    name: Process Data
    type: bash
    script: echo "Processing B"

outputs:
  result:
    node: process
